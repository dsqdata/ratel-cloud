spring:
  application:
    name: rc-monitor-admin-server
  cloud:
    nacos:
      discovery:
        # 配置中心地址
        server-addr: localhost:8848
        namespace: ratel-cloud
        group: DEFAULT_GROUP
        metadata:
          VERSION: 1.0
          user.name: "admin"
          user.password: "admin"
      config:
        # 配置中心地址
        server-addr: localhost:8848
        namespace: ratel-cloud
        group: DEFAULT_GROUP
        # 指定配置文件格式
        file-extension: yaml


